# This is a config for E2B sandbox template.
# You can use template ID (seghowwiybrjbsvsyprb) or template name (test-again) to create a sandbox:

# Python SDK
# from e2b import Sandbox, AsyncSandbox
# sandbox = Sandbox.create("test-again") # Sync sandbox
# sandbox = await AsyncSandbox.create("test-again") # Async sandbox

# JS SDK
# import { Sandbox } from 'e2b'
# const sandbox = await Sandbox.create('test-again')

team_id = "c55d64da-9dfc-4969-a81a-b1ea9f4df688"
start_cmd = "/compile_page.sh"
dockerfile = "e2b.Dockerfile"
template_name = "test-again"
template_id = "seghowwiybrjbsvsyprb"
